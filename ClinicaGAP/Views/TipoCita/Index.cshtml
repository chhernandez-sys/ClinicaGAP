@model IEnumerable<ClinicaGAP.Models.TIPO_CITA>

@{
    ViewBag.Title = "Index";
}

<h2>Administración de tipos de cita.</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTADO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTADO1.DESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_TIPO_CITA }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ID_TIPO_CITA }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_TIPO_CITA })
        </td>
    </tr>
}

</table>